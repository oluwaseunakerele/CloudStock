spring.application.name=CloudStock

# Cloud SQL (MySQL) via Socket Factory
spring.datasource.url=jdbc:mysql://cloudstock.creqq0ckm4q1.us-east-2.rds.amazonaws.com:3306/cloudstock?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.username=admin
spring.datasource.password=Samopel2002
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# --- JPA/Hibernate ---
spring.jpa.hibernate.ddl-auto=validate   # use 'update' if you want Hibernate to create/alter tables
spring.jpa.database-platform=org.hibernate.dialect.MySQL8Dialect
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# --- Port: EB injects PORT; fallback 8080 locally ---
server.port=${PORT:5000}